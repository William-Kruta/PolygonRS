[project]
name = "polygonrs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "polars>=1.41.2",
    "polygon",
    "pyarrow>=24.0.0",
    "pytz>=2026.2",
]
